<table class="room_user_list" id="room_user_list">
  <tr>
    <th>ルームユーザー名</th>
    <th></th>
  </tr>
  <% room_users.each do | room_user | %>
    <% @user = User.find(room_user.user_id) %>
    <tr>
      <th><%= @user.name %></th>
      <th><%= @user.chip %></th>
    </tr>
  <% end %>
</table>
